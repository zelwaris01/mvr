import { TourScreen } from "@/app/_components/tour/TourScreen";

/**
 * The whole app. The 360° visit is the product; everything else is an overlay
 * drawn on top of it.
 */
export default function Page() {
  return <TourScreen />;
}
